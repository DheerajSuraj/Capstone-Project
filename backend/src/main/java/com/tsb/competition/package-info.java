/**
 * Competition lifecycle (DRAFT -> OPEN -> LOCKED -> EVALUATING -> PUBLISHED),
 * submissions pinned to immutable strategy versions, batch evaluation on
 * frozen datasets, out-of-sample scoring, guardrails, leaderboards.
 * (Roadmap §11)
 */
package com.tsb.competition;
